// src/features/transactions/index.ts
// Barrel export — re-exporta TransactionsManager como default para callers externos
export { default } from './TransactionsManager';
export { calculateTransactionTotals } from './transactionGroupUtils';
